// Stub — this file should never be imported directly.
// Use database_connection.dart which picks the right implementation.
import 'package:drift/drift.dart';

QueryExecutor openConnection() {
  throw UnsupportedError('Platform not supported');
}
