// User interface - compiled JS was empty (interface-only file, erased at compile time)
export interface IUser {
  id: string;
  username: string;
  password: string;
  active: boolean;
  firstName: string;
  lastName: string;
  email: string;
  tenantId: string;
}
